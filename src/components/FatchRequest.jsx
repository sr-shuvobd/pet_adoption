import React from 'react';
import Requset from "@/components/Requset";

const FatchRequest = ({id}) => {
    return (
        <div>
            <Request/>
        </div>
    );
};

export default FatchRequest;